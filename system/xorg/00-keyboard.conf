# Server-level keymap: applies to every keyboard, including ones hotplugged
# later (dock). setxkbmap in xinitrc only touches keyboards present at login.
Section "InputClass"
        Identifier "system-keyboard"
        MatchIsKeyboard "on"
        Option "XkbLayout" "us"
        Option "XkbOptions" "caps:swapescape"
EndSection
