# New keyboard appeared (dock plug included): reapply the session xkb options.
# Belt-and-braces for X servers started before xorg/00-keyboard.conf existed.
ACTION=="add", SUBSYSTEM=="input", ENV{ID_INPUT_KEYBOARD}=="1", RUN+="/usr/bin/systemd-run --no-block /usr/local/bin/xkb-hotplug"
