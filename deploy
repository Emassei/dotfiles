#!/bin/bash
#
# Bootstrap a new Arch machine to match Ernie's working setup.
# Idempotent — safe to re-run.
#
# What this does:
#   1. Installs pacman packages (core OS + dev tools + DWM stack)
#   2. Installs paru (AUR helper) if missing
#   3. Installs AUR packages
#   4. Clones + builds suckless tools (dwm, dwmblocks, dmenu)
#   5. Links the vendored nvim config (NvChad-based, dotfiles/nvim)
#   6. Sets up tmux TPM
#   7. Initializes git submodules (oh-my-zsh)
#   8. Creates dotfile symlinks
#   9. Installs Google Cloud SDK (official, not AUR)
#  10. Installs herdr (release binary) + system files (NetworkManager dispatcher, reflector config)
#  11. Enables systemd timers (reflector, fstrim, autorandr)
#  12. Sets shell to zsh

set -e

DOTFILES="$HOME/dotfiles"
CODE_DIR="$HOME/code"
SUCKLESS_DIR="$CODE_DIR/suckless"

GREEN='\033[0;32m'; YELLOW='\033[1;33m'; RED='\033[0;31m'; NC='\033[0m'
info() { echo -e "${GREEN}[INFO]${NC} $1"; }
warn() { echo -e "${YELLOW}[WARN]${NC} $1"; }
error() { echo -e "${RED}[ERROR]${NC} $1"; }

# --yes / DEPLOY_YES=1: non-interactive (ripcord, rehearsals)
AUTO_YES=${DEPLOY_YES:-0}
for _a in "$@"; do case "$_a" in -y|--yes) AUTO_YES=1;; esac; done

confirm() {
    [[ "$AUTO_YES" == 1 ]] && return 0
    read -r -p "$1 [y/N] " response
    [[ "$response" =~ ^[Yy]$ ]]
}

# =============================================================================
# Git repositories (your forks of suckless tools + NvChad)
# =============================================================================
declare -A suckless_repos=(
    [dwm]='git@github.com:Emassei/dwm.git'
    [dwmblocks]='git@github.com:Emassei/dwmblocks.git'
    [dmenu]='git@github.com:Emassei/dmenu.git'
)

# =============================================================================
# Package lists — generated files, the machine is the source of truth (see pkglist.txt)
# =============================================================================
PACKAGES=( $(grep -vE "^#|^$" "$DOTFILES/pkglist.txt") )

AUR_PACKAGES=( $(grep -vE "^#|^$" "$DOTFILES/aurlist.txt") )

# =============================================================================
# Functions
# =============================================================================
check_arch() {
    if ! command -v pacman &> /dev/null; then
        error "This script is for Arch Linux only."
        exit 1
    fi
}

check_ssh() {
    if ! ssh -T git@github.com 2>&1 | grep -q "successfully authenticated"; then
        error "SSH authentication to GitHub failed."
        error "Set up your SSH keys first, then re-run this script."
        exit 1
    fi
}

refresh_mirrorlist() {
    info "Refreshing pacman mirrorlist with reflector..."
    command -v reflector >/dev/null || sudo pacman -Sy --needed --noconfirm reflector   # fresh installs don't have it yet
    sudo reflector --country 'United States' --age 6 --latest 20 \
        --protocol https --sort rate --save /etc/pacman.d/mirrorlist
}

install_pacman_packages() {
    info "Installing pacman packages..."
    sudo pacman -Syu --needed --noconfirm "${PACKAGES[@]}"
}

install_paru() {
    if command -v paru &> /dev/null; then
        info "paru already installed"
        return
    fi
    info "Installing paru (AUR helper)..."
    mkdir -p "$CODE_DIR"
    git clone https://aur.archlinux.org/paru.git "$CODE_DIR/paru"
    (cd "$CODE_DIR/paru" && makepkg -si --noconfirm)
}

install_aur_packages() {
    info "Installing AUR packages..."
    # makepkg defaults to -j1; a fresh box would compile every AUR package single-threaded
    mkdir -p "$HOME/.config/pacman"
    grep -q '^MAKEFLAGS' "$HOME/.config/pacman/makepkg.conf" 2>/dev/null || \
        printf 'MAKEFLAGS="-j$(nproc)"\n' >> "$HOME/.config/pacman/makepkg.conf"
    # One broken AUR package (a -bin whose upstream URL moved, a checksum drift) must not
    # abort the whole deploy: try the batch, then fall back to one-by-one and report failures.
    if ! paru -S --needed --noconfirm "${AUR_PACKAGES[@]}"; then
        warn "AUR batch failed — retrying one package at a time"
        FAILED_AUR=()
        for pkg in "${AUR_PACKAGES[@]}"; do
            paru -S --needed --noconfirm "$pkg" || FAILED_AUR+=("$pkg")
        done
        if ((${#FAILED_AUR[@]})); then
            warn "AUR packages that FAILED (install by hand later): ${FAILED_AUR[*]}"
        fi
    fi
}

setup_directories() {
    info "Creating directory structure..."
    mkdir -p "$CODE_DIR" "$SUCKLESS_DIR"
    mkdir -p "$HOME/.config/zsh"
    mkdir -p "$HOME/.config/alacritty"
    mkdir -p "$HOME/.config/zathura"
    mkdir -p "$HOME/.config/ranger"
    mkdir -p "$HOME/.config/clipmenu"
    mkdir -p "$HOME/.config/herdr"
    mkdir -p "$HOME/.config/dunst" "$HOME/.config/flameshot" "$HOME/.config/fontconfig"
    mkdir -p "$HOME/.config/restic" "$HOME/.config/systemd/user"
    mkdir -p "$HOME/.local/share/zsh/history"
    mkdir -p "$HOME/.local/bin"
}

clone_or_pull() {
    local repo=$1 dest=$2
    if [[ -d "$dest/.git" ]]; then
        info "Updating $dest..."
        git -C "$dest" pull --ff-only || warn "$dest pull skipped (dirty/diverged)"
    else
        info "Cloning $repo to $dest..."
        git clone "$repo" "$dest"
    fi
}

setup_tmux() {
    info "Setting up tmux plugin manager..."
    local tpm_dir="$HOME/.tmux/plugins/tpm"
    if [[ ! -d "$tpm_dir" ]]; then
        git clone https://github.com/tmux-plugins/tpm "$tpm_dir"
    else
        info "TPM already installed"
    fi
}

setup_suckless() {
    info "Setting up suckless tools (dwm, dwmblocks, dmenu)..."
    for tool in "${!suckless_repos[@]}"; do
        clone_or_pull "${suckless_repos[$tool]}" "$SUCKLESS_DIR/$tool"
        info "Compiling $tool..."
        (cd "$SUCKLESS_DIR/$tool" && sudo make clean install)
    done
}

setup_nvchad() {
    info "Linking nvim config (vendored NvChad, dotfiles/nvim)..."
    create_symlink "$DOTFILES/nvim" "$HOME/.config/nvim"
}

create_symlink() {
    local src=$1 dest=$2
    if [[ -L "$dest" ]]; then
        info "Symlink already exists: $dest"
    elif [[ -e "$dest" ]]; then
        warn "Backing up existing $dest to ${dest}.bak"
        mv "$dest" "${dest}.bak"
        ln -s "$src" "$dest"
        info "Created symlink: $dest -> $src"
    else
        ln -s "$src" "$dest"
        info "Created symlink: $dest -> $src"
    fi
}

create_wrapper() {
    # For files that carry machine-local additions (secrets, work aliases):
    # write a tiny file that SOURCES the repo copy instead of symlinking it,
    # so local lines survive re-runs. No-op if the wrapper already sources it.
    local src=$1 dest=$2
    if [[ -f "$dest" ]] && grep -qF "$src" "$dest"; then
        info "Wrapper already sources $src: $dest"
    elif [[ -e "$dest" && ! -L "$dest" ]]; then
        warn "Backing up existing $dest to ${dest}.bak"
        mv "$dest" "${dest}.bak"
        printf 'source %s\n' "$src" > "$dest"
        info "Created wrapper: $dest -> source $src"
    else
        [[ -L "$dest" ]] && rm "$dest"
        printf 'source %s\n' "$src" > "$dest"
        info "Created wrapper: $dest -> source $src"
    fi
}

setup_symlinks() {
    info "Creating symlinks..."
    # Wrappers (source the repo file; keep local secrets/work lines alongside)
    create_wrapper "$DOTFILES/zsh/zshrc_manager.sh" "$HOME/.config/zsh/.zshrc"
    for extra in .zshrc.secrets .zshrc.work; do
        grep -qF "$extra" "$HOME/.config/zsh/.zshrc" || \
            printf '[ -f ~/.config/zsh/%s ] && source ~/.config/zsh/%s\n' "$extra" "$extra" >> "$HOME/.config/zsh/.zshrc"
    done
    create_wrapper "$DOTFILES/zprofile" "$HOME/.zprofile"
    mkdir -p "$HOME/.config/tmux"
    create_wrapper "$DOTFILES/tmux/tmux.conf" "$HOME/.config/tmux/tmux.conf"
    create_symlink "$DOTFILES/alacritty/alacritty.toml" "$HOME/.config/alacritty/alacritty.toml"
    create_symlink "$DOTFILES/gitconfig/.gitconfig" "$HOME/.gitconfig"
    create_symlink "$DOTFILES/xinitrc_dwm" "$HOME/.xinitrc"
    create_symlink "$DOTFILES/xbindkeys" "$HOME/.xbindkeysrc"
    create_symlink "$DOTFILES/Xresources" "$HOME/.Xresources"
    create_symlink "$DOTFILES/apps/zathura/zathurarc" "$HOME/.config/zathura/zathurarc"
    create_symlink "$DOTFILES/apps/ranger/rc.conf" "$HOME/.config/ranger/rc.conf"
    create_symlink "$DOTFILES/apps/ranger/rifle.conf" "$HOME/.config/ranger/rifle.conf"
    create_symlink "$DOTFILES/apps/ranger/scope.sh" "$HOME/.config/ranger/scope.sh"
    create_symlink "$DOTFILES/apps/ranger/commands.py" "$HOME/.config/ranger/commands.py"
    create_symlink "$DOTFILES/herdr/config.toml" "$HOME/.config/herdr/config.toml"
    create_symlink "$DOTFILES/apps/dunst/dunstrc" "$HOME/.config/dunst/dunstrc"
    # flameshot rewrites its ini on every settings change (breaks symlinks): seed once, don't link.
    [[ -e "$HOME/.config/flameshot/flameshot.ini" ]] || cp "$DOTFILES/apps/flameshot/flameshot.ini" "$HOME/.config/flameshot/flameshot.ini"
    create_symlink "$DOTFILES/apps/fontconfig/fonts.conf" "$HOME/.config/fontconfig/fonts.conf"
    create_symlink "$DOTFILES/apps/greenclip/greenclip.toml" "$HOME/.config/greenclip.toml"
    create_symlink "$DOTFILES/apps/autorandr" "$HOME/.config/autorandr"
    create_symlink "$DOTFILES/restic/excludes" "$HOME/.config/restic/excludes"
}

setup_user_units() {
    info "Installing user systemd units (restic backup, rclone mounts, phone)..."
    for u in "$DOTFILES"/systemd/user/*; do
        create_symlink "$u" "$HOME/.config/systemd/user/$(basename "$u")"
    done
    systemctl --user daemon-reload
    systemctl --user enable restic-backup.timer 2>/dev/null || true
    warn "restic needs ~/.config/restic/password (from KeePass) before the timer can run"
}

setup_zsh() {
    if [[ "$SHELL" != *"zsh"* ]]; then
        info "Changing default shell to zsh..."
        chsh -s "$(which zsh)"
    else
        info "zsh is already the default shell"
    fi
    touch "$HOME/.local/share/zsh/history/.zsh_history"
}

setup_herdr() {
    # herdr (agent workspace manager) is a release binary, not a package.
    # Installer: stable channel, drops the binary in ~/.local/bin. `herdr update` later.
    if ! command -v herdr >/dev/null && [[ ! -x "$HOME/.local/bin/herdr" ]]; then
        info "Installing herdr..."
        curl -fsSL https://herdr.dev/install.sh | sh
    else
        info "herdr already installed"
    fi
    # fleet scripts call herdr over non-interactive ssh, where ~/.local/bin isn't on PATH
    [[ -e /usr/local/bin/herdr ]] || sudo ln -s "$HOME/.local/bin/herdr" /usr/local/bin/herdr
}

install_gcloud() {
    if [[ -d "$HOME/.local/opt/google-cloud-sdk" ]]; then
        info "gcloud SDK already installed"
        return
    fi
    info "Installing Google Cloud SDK (official, not AUR)..."
    mkdir -p "$HOME/.local/opt"
    curl -sSL https://sdk.cloud.google.com -o /tmp/gcloud_install.sh
    bash /tmp/gcloud_install.sh --disable-prompts --install-dir="$HOME/.local/opt"
    rm -f /tmp/gcloud_install.sh
}

install_system_files() {
    info "Installing system files..."

    # NetworkManager: turn wifi off when ethernet is connected
    if [[ -f "$DOTFILES/system/NetworkManager-dispatcher/10-wifi-off-on-ethernet" ]]; then
        sudo install -o root -g root -m 755 \
            "$DOTFILES/system/NetworkManager-dispatcher/10-wifi-off-on-ethernet" \
            /etc/NetworkManager/dispatcher.d/10-wifi-off-on-ethernet
    fi

    # Pacman hook: auto-trim cache after every transaction (keep last 2 versions)
    if [[ -f "$DOTFILES/system/pacman-hooks/auto-paccache.hook" ]]; then
        sudo mkdir -p /etc/pacman.d/hooks
        sudo install -o root -g root -m 644 \
            "$DOTFILES/system/pacman-hooks/auto-paccache.hook" \
            /etc/pacman.d/hooks/auto-paccache.hook
    fi

    # acpid lid hook: lid open at dock = dual head, closed = monitor only
    if [[ -d "$DOTFILES/system/acpi" ]]; then
        sudo install -o root -g root -m 755 \
            "$DOTFILES/system/acpi/lid-layout.sh" /etc/acpi/lid-layout.sh
        sudo mkdir -p /etc/acpi/events
        sudo install -o root -g root -m 644 \
            "$DOTFILES/system/acpi/lid-layout.rules" /etc/acpi/events/lid-layout
    fi

    # udev: display hotplug (dock plug/unplug) reruns the same layout hook
    if [[ -f "$DOTFILES/system/udev/95-monitor-hotplug.rules" ]]; then
        sudo install -o root -g root -m 644 \
            "$DOTFILES/system/udev/95-monitor-hotplug.rules" /etc/udev/rules.d/
    fi

    # Weekly update stager: pre-download upgrades + Arch news check, notify
    if [[ -d "$DOTFILES/system/pacman-stage" ]]; then
        sudo install -o root -g root -m 755 \
            "$DOTFILES/system/pacman-stage/pacman-stage" /usr/local/bin/pacman-stage
        sudo install -o root -g root -m 644 \
            "$DOTFILES/system/pacman-stage/pacman-stage.service" \
            "$DOTFILES/system/pacman-stage/pacman-stage.timer" \
            /etc/systemd/system/
    fi

    # systemd user services: rclone gdrive mounts
    if [[ -d "$DOTFILES/system/systemd-user" ]]; then
        mkdir -p "$HOME/.config/systemd/user"
        cp "$DOTFILES/system/systemd-user/"*.service "$HOME/.config/systemd/user/"
        # Ensure mount points exist (services AssertPathIsDirectory)
        mkdir -p "$HOME/gdrive"
        mkdir -p "$HOME/code/python/philosophy/kindle-notes"
    fi

    # Reflector config: keep mirrorlist fresh weekly
    sudo tee /etc/xdg/reflector/reflector.conf > /dev/null <<'EOF'
--save /etc/pacman.d/mirrorlist
--country 'United States'
--age 6
--latest 20
--protocol https
--sort rate
EOF
}

enable_services() {
    info "Enabling systemd timers and services..."
    sudo systemctl enable --now pcscd.socket   # YubiKey (ykman oath) talks through pcscd

    # Weekly mirrorlist refresh
    sudo systemctl enable --now reflector.timer

    # Weekly SSD TRIM
    sudo systemctl enable --now fstrim.timer

    # Weekly update staging (download-only + notify; install stays manual)
    sudo systemctl enable --now pacman-stage.timer

    # Lid open/close reruns the screen layout (twoscreens via acpid)
    sudo systemctl enable --now acpid.service

    # Auto-switch monitor profile on plug/unplug
    sudo systemctl enable autorandr.service

    # NetworkManager dispatcher
    sudo systemctl enable --now NetworkManager-dispatcher.service

    # User services
    systemctl --user enable --now greenclip.service

    # rclone mounts: enable only (needs `rclone config` first to set up auth)
    if [[ -f "$HOME/.config/rclone/rclone.conf" ]]; then
        systemctl --user enable --now rclone-gdrive-t490.service rclone-kindle-notes.service 2>/dev/null || \
            warn "rclone services not started — run 'rclone config' first, then enable manually"
    else
        warn "~/.config/rclone/rclone.conf missing — skip rclone services."
        warn "Run 'rclone config' to set up 'gdrive' remote, then:"
        warn "  systemctl --user enable --now rclone-gdrive-t490.service rclone-kindle-notes.service"
    fi

    # Time sync (needed for Yubikey TOTP)
    sudo timedatectl set-ntp true
}

setup_system() {
    info "Configuring system settings..."
    # Arch logo in /etc/issue
    if [[ -f "$DOTFILES/arch_logo" ]]; then
        sudo cp "$DOTFILES/arch_logo" /etc/issue
    fi
}

# =============================================================================
# Main
# =============================================================================
main() {
    echo "========================================"
    echo "  Ernie's Dotfiles Deploy Script"
    echo "========================================"
    echo ""
    echo "This will install and configure:"
    echo "  - pacman + AUR packages (DWM stack, dev tools, fonts)"
    echo "  - Suckless tools (dwm, dwmblocks, dmenu)"
    echo "  - nvim config (vendored, dotfiles/nvim)"
    echo "  - Alacritty, Zsh, tmux configs"
    echo "  - Google Cloud SDK (official installer)"
    echo "  - NetworkManager dispatcher (wifi off on ethernet)"
    echo "  - Systemd timers: reflector (weekly), fstrim (weekly), autorandr"
    echo ""
    echo "After deploy, save autorandr profiles per machine:"
    echo "  - With external monitor:     autorandr --save docked"
    echo "  - On laptop alone:           autorandr --save mobile"
    echo ""

    if ! confirm "Continue?"; then
        echo "Aborted."
        exit 1
    fi

    check_arch
    check_ssh
    refresh_mirrorlist
    setup_directories
    install_pacman_packages
    install_paru
    install_aur_packages
    setup_tmux
    setup_suckless
    setup_nvchad
    setup_symlinks
    setup_user_units
    setup_herdr
    install_gcloud
    install_system_files
    enable_services
    setup_zsh
    setup_system

    echo ""
    echo "========================================"
    info "Done. Log out and back in for shell changes."
    info "Then save autorandr profiles (see above)."
    echo "========================================"
}

main "$@"
