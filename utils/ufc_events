#!/bin/sh

curl -s "https://en.wikipedia.org/wiki/List_of_UFC_events" | xpath -q -e '//table[@id="Scheduled_events"]//td' | sed "/reference/d; /<\/td>/d; /mw-redirect/d; /rowspan/d; s/<td>//g; s/<[^>]\+>/ /g" > $XDG_CACHE_HOME/ufc_events.html

cat $XDG_CACHE_HOME/ufc_events.html

